# Context Agent — alias

Compatibility shim: some older specs and prompts call this role `context.md`.
The canonical definition lives in [`context-agent.md`](context-agent.md) and is what
`config/agents.yaml` + `runtime/agent_router.py` resolve for `phase: context`.

Read `context-agent.md`; do not maintain two copies.
