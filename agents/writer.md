# Writer Agent — alias

Compatibility shim for the role name used in `PRP.md` §4. The canonical definition
lives in [`synthesizer.md`](synthesizer.md); `runtime/agent_router.py` resolves
`writer.md` → `synthesizer`.

Read `synthesizer.md`; do not maintain two copies.
