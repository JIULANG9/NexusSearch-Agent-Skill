#!/usr/bin/env python3
"""Zero-install launcher: ``./nexus <subcommand>`` from the skill root."""

import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))
from runtime.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
